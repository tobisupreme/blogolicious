
describe('The owner of a blog', () => {
  it('should be able to update the state of the blog to published', async () => {
    //
  })

  it('should be able to edit the blog in draft or published state', async () => {
    //
  })

  it('should be able to delete the blog in draft or published state', async () => {
    //
  })

  it('should be able to get a list of their blogs in pages with a max of 20 per page', async () => {
    //
  })
})

describe('When a single blog is requested', () => {
  it('the api should return the user information with the blog', async () => {
    //
  })

  it('the read_count should increase by 1', () => {
    //
  })
})